extends layout

block content
  .container
    h2 #{user} posts  

    if blogPosts
      each post in blogPosts
        .card
          .card-body
            a(href="/post/" + post.posturl).card-title #{post.posttitle}
            p.card-text= post.postbody
            a(href='/post/auth/' + post.user.username)= post.user.username
    
    
       
